# SMTP Configuration for ABKHAS AI
# Copy this file to .env.local and fill in your SMTP credentials

# ============================================
# SMTP Server Configuration
# ============================================

# SMTP Host (e.g., smtp.gmail.com, smtp.mailgun.org)
VITE_SMTP_HOST=smtp.gmail.com

# SMTP Port (587 for TLS, 465 for SSL)
VITE_SMTP_PORT=587

# Email account for authentication
VITE_SMTP_USER=your-email@gmail.com

# Email password or app-specific password
VITE_SMTP_PASSWORD=your-app-password

# Sender email address
VITE_SMTP_FROM=noreply@abkhas.ai

# Sender name (displayed in emails)
VITE_SMTP_FROM_NAME=ABKHAS AI

# ============================================
# Optional: Backend API for Email Sending
# ============================================

# If you prefer to send emails via backend API
VITE_API_ENDPOINT=https://api.abkhas.ai

# API key for email service (if using backend)
VITE_EMAIL_API_KEY=your-api-key-here

# ============================================
# Email Service Provider (Optional)
# ============================================

# Uncomment if using a specific provider
# VITE_EMAIL_PROVIDER=gmail
# VITE_EMAIL_PROVIDER=mailgun
# VITE_EMAIL_PROVIDER=sendgrid
# VITE_EMAIL_PROVIDER=aws-ses
