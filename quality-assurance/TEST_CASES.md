# حالات الاختبار التفصيلية - تطبيق ابخص

## 📋 فهرس حالات الاختبار

1. [اختبارات الوحدات (Unit Tests)](#unit-tests)
2. [اختبارات التكامل (Integration Tests)](#integration-tests)
3. [اختبارات النظام E2E (System Tests)](#system-tests)
4. [اختبارات قبول المستخدم (UAT)](#uat-tests)
5. [السيناريوهات السلبية (Negative Scenarios)](#negative-scenarios)

---

## <a id="unit-tests"></a>🔬 1. اختبارات الوحدات (Unit Tests)

### 1.1 محرك التقييم والمقارنة (Scoring Engine)

#### TC-UNIT-001: حساب السعر الصافي
```typescript
Test ID: TC-UNIT-001
Module: services/scoring.ts
Function: calculateNetPrice()

Given: منتج بالبيانات التالية:
  - السعر: 5000 ريال
  - الشحن: 50 ريال
  - كوبون خصم: 200 ريال

When: استدعاء calculateNetPrice(offer)

Then: 
  Expected: 4850 ريال
  Actual: ?
  Status: ✅ PASS / ❌ FAIL

Business Impact: دقة هذه الدالة تؤثر مباشرة على ترتيب الصفقات → تؤثر على معدل النقر بنسبة 15%+
```

#### TC-UNIT-002: ترتيب حسب الأولوية "السعر"
```typescript
Test ID: TC-UNIT-002
Module: services/scoring.ts
Function: calculateProductScore()

Given: 
  - User Preference: priority = 'price'
  - المنتج A: سعر 3000 ريال، تقييم 4.2
  - المنتج B: سعر 3500 ريال، تقييم 4.9

When: calculateProductScore(productA) vs calculateProductScore(productB)

Then: 
  Expected: scoreA > scoreB (المنتج الأرخص يجب أن يكون له سكور أعلى)
  
Business Impact: إذا لم يتم ترتيب المنتجات حسب تفضيلات المستخدم → انخفاض الثقة → -20% في معدل العودة
```

#### TC-UNIT-003: استبعاد المنتجات خارج الميزانية
```typescript
Test ID: TC-UNIT-003
Module: services/scoring.ts
Function: calculatePriceScore()

Given:
  - ميزانية المستخدم: 2000 - 4000 ريال
  - منتج بسعر: 5000 ريال (خارج الميزانية بـ 25%)

When: calculatePriceScore(5000, {min: 2000, max: 4000})

Then:
  Expected: 0.0 (يجب استبعاده)
  
Business Impact: عرض منتجات خارج الميزانية = إحباط المستخدم → -30% في الرضا
```

#### TC-UNIT-004: حساب سكور الموثوقية
```typescript
Test ID: TC-UNIT-004
Module: services/scoring.ts
Function: calculateTrustScore()

Given:
  - Offer A: تقييم 4.8، عدد التقييمات: 1500، متجر موثّق ✅
  - Offer B: تقييم 5.0، عدد التقييمات: 10، متجر غير موثّق

When: calculateTrustScore(offerA) vs calculateTrustScore(offerB)

Then:
  Expected: trustScoreA > trustScoreB
  Reason: العدد الكبير من التقييمات + التوثيق أهم من التقييم الكامل مع عينة صغيرة
  
Business Impact: سكور الموثوقية الدقيق = +25% في معدل التحويل
```

### 1.2 محرك البحث والمطابقة

#### TC-UNIT-005: البحث مع أخطاء إملائية
```typescript
Test ID: TC-UNIT-005
Module: utils/searchUtils.ts
Function: calculateMatchScore()

Test Cases:
  1. query="ايفون" → يجب أن يطابق "iPhone"
  2. query="سامسونج" → يجب أن يطابق "Samsung"
  3. query="ماك بوك" → يجب أن يطابق "MacBook"

When: calculateMatchScore(query, product)

Then:
  Expected: relevance > 0.8 (تطابق قوي)
  
Business Impact: التعامل مع الأخطاء الإملائية = +40% في نتائج البحث الناجحة
```

#### TC-UNIT-006: البحث بالعربية والإنجليزية
```typescript
Test ID: TC-UNIT-006
Module: utils/searchUtils.ts

Test Cases:
  1. query="ايفون 15 برو" → يجب أن يطابق "iPhone 15 Pro"
  2. query="iPhone" → يجب أن يطابق منتجات Apple
  3. query="هواوي" → يجب أن يطابق "Huawei"

Expected: نفس النتائج بغض النظر عن اللغة

Business Impact: دعم اللغتين = +50% في قاعدة المستخدمين المحتملين
```

### 1.3 خدمة الأفلييت

#### TC-UNIT-007: توليد روابط الأفلييت
```typescript
Test ID: TC-UNIT-007
Module: services/affiliateService.ts
Function: generateAffiliateLink()

Test Cases:
  1. storeId="jarir", url="https://jarir.com/product"
     → Expected: يحتوي على affiliate parameter
  
  2. storeId="noon", url="https://noon.com/item"
     → Expected: يحتوي على ref code
  
  3. storeId="amazon", url="https://amazon.sa/dp/123"
     → Expected: يحتوي على tag parameter

Then: الروابط يجب أن تكون صالحة وتحتوي على معرّف ابخص

Business Impact: روابط الأفلييت الصحيحة = تتبع دقيق للعمولات = الإيرادات الأساسية
```

---

## <a id="integration-tests"></a>🔗 2. اختبارات التكامل (Integration Tests)

### 2.1 تدفق البيانات من API إلى UI

#### TC-INT-001: جلب العروض من المتاجر وعرضها
```typescript
Test ID: TC-INT-001
Components: API Layer → Data Processing → UI Rendering

Given: المستخدم يبحث عن "سماعات"

When: 
  1. النظام يستدعي APIs المتاجر (جرير، نون، أمازون)
  2. يجمع البيانات ويخزنها في الذاكرة المؤقتة
  3. يطبق خوارزمية الترتيب
  4. يعرض النتائج في واجهة المستخدم

Then:
  - يجب أن تظهر النتائج في < 2 ثانية
  - يجب أن تكون البيانات متسقة (نفس الأسعار من API)
  - يجب أن تكون الصفقة الأولى هي الأفضل فعلياً

Assert:
  ✓ Response time < 2000ms
  ✓ Data integrity: API price === UI price
  ✓ First product has highest score

Business Impact: التكامل السلس = تجربة سريعة = +15% في معدل التحويل
```

#### TC-INT-002: تطبيق الفلاتر وإعادة الترتيب
```typescript
Test ID: TC-INT-002
Components: Filter UI → Search Logic → Data Re-ranking

Given: مستخدم في صفحة نتائج البحث (50 منتج)

When:
  1. يختار فلتر "الميزانية: 1000-2000 ريال"
  2. يختار فلتر "العلامة: Samsung"
  3. يختار الترتيب: "من الأرخص للأغلى"

Then:
  - يجب أن تختفي كل المنتجات خارج الميزانية
  - يجب أن تظهر فقط منتجات Samsung
  - يجب أن يكون الترتيب تصاعدياً حسب السعر

Assert:
  ✓ Filtered results count < Original results
  ✓ All results match filters
  ✓ Sorting is correct

Business Impact: فلترة دقيقة = وصول سريع للصفقة المطلوبة = +30% في الرضا
```

### 2.2 تتبع التفاعلات والعمولات

#### TC-INT-003: النقر على صفقة وتتبع Conversion
```typescript
Test ID: TC-INT-003
Components: Product Card → Affiliate Service → Analytics

Given: مستخدم يرى صفقة من متجر نون

When:
  1. يضغط على زر "شوف الصفقة"
  2. النظام يولد رابط أفلييت
  3. يفتح المتجر في نافذة جديدة
  4. يسجل الحدث في Google Analytics

Then:
  ✓ الرابط يحتوي على معرّف الأفلييت
  ✓ يُفتح في نافذة جديدة (target="_blank")
  ✓ يتم تسجيل event: "product_click"
  ✓ يتم حفظ التفاعل في interactions[]

Business Impact: التتبع الدقيق = إثبات العمولات = ضمان الإيرادات
```

#### TC-INT-004: معالجة فشل API أحد المتاجر
```typescript
Test ID: TC-INT-004
Components: API Handlers → Error Handling → UI Feedback

Given: API متجر جرير لا يستجيب (timeout أو 500 error)

When: المستخدم يبحث عن "تلفزيون"

Then:
  ✓ يعرض النتائج من المتاجر الأخرى (نون، أمازون، إكسترا)
  ✓ يظهر تنبيه للمستخدم: "بعض المتاجر غير متوفرة حالياً"
  ✓ لا يتوقف التطبيق عن العمل
  ✓ يسجل الخطأ في نظام المراقبة (Sentry)

Assert:
  ✓ Results.length > 0
  ✓ Error notification displayed
  ✓ No app crash

Business Impact: التعامل الصحيح مع الأخطاء = استمرارية الخدمة = حفاظ على المستخدمين
```

---

## <a id="system-tests"></a>🌐 3. اختبارات النظام E2E (End-to-End)

### 3.1 رحلة مستخدم كاملة: من البحث إلى النقر

#### TC-E2E-001: البحث عن منتج واختيار أفضل صفقة
```gherkin
Test ID: TC-E2E-001
Type: Critical User Journey
Priority: P0 (Blocker)

Feature: البحث عن أفضل صفقة ايفون
  
  Background:
    Given المستخدم فتح تطبيق "ابخص"
    And اللغة: العربية
    And الميزانية: 4000-6000 ريال
  
  Scenario: بحث ناجح عن ايفون 15 برو
    Given المستخدم في الصفحة الرئيسية
    
    When يكتب "ايفون 15 برو" في خانة البحث
    And يضغط على زر البحث أو Enter
    
    Then يجب أن يرى صفحة نتائج البحث في أقل من 2 ثانية
    And يجب أن يرى على الأقل 3 نتائج
    And يجب أن تكون الصفقة الأولى مميزة بشارة "⚡ أفضل صفقة"
    
    When يقرأ "تحليل أبخص الذكي" للصفقة الأولى
    Then يجب أن يفهم لماذا هذه أفضل صفقة (مثال: "الأرخص بـ 300 ريال مع شحن مجاني")
    
    When يضغط على "شوف الصفقة"
    Then يجب أن يُفتح موقع المتجر في نافذة جديدة
    And يجب أن يحتوي الرابط على معرّف الأفلييت
    And يجب أن يتم تسجيل الحدث في Analytics

  Expected Outcome:
    ✅ المستخدم وصل للصفقة الأفضل في أقل من 3 نقرات
    ✅ الثقة في التوصية: عالية (بسبب الشرح الواضح)
    ✅ احتمالية الشراء: مرتفعة

  Business KPIs:
    - Time to Best Deal: < 10 ثوانٍ
    - Clicks to Conversion: ≤ 3
    - User Satisfaction: 4.5/5

  Revenue Impact: 
    هذه الرحلة تمثل 60% من إجمالي عمولات التطبيق
```

#### TC-E2E-002: استخدام الفلاتر للتضييق
```gherkin
Test ID: TC-E2E-002
Type: Standard User Journey
Priority: P1 (High)

Feature: تصفية النتائج حسب الميزانية والعلامة
  
  Scenario: مستخدم يريد سماعات Apple بميزانية محددة
    Given المستخدم بحث عن "سماعات"
    And رأى 30 نتيجة
    
    When يفتح قائمة الفلاتر
    And يختار "الميزانية: 500-1000 ريال"
    And يختار "العلامة: Apple"
    
    Then يجب أن تنخفض النتائج إلى العدد المطابق فقط
    And يجب أن تكون كل النتائج من Apple
    And يجب أن تكون الأسعار ضمن النطاق 500-1000
    And يجب أن تُرتّب حسب "الأفضل" (AI Score)
    
    When يضغط على "الترتيب: من الأرخص للأغلى"
    Then يجب أن يتغير الترتيب فوراً بدون إعادة تحميل الصفحة

  Assert:
    ✓ Filter response time < 500ms
    ✓ Results match all filters
    ✓ No page reload

  Business Impact: فلترة سريعة = وصول أسرع = +25% CTR
```

### 3.2 رحلة العودة والإشعارات

#### TC-E2E-003: مستخدم يعود ليرى تنبيهات العروض الجديدة
```gherkin
Test ID: TC-E2E-003
Type: Retention Journey
Priority: P1 (High)

Feature: إشعارات العروض الجديدة

  Scenario: مستخدم يعود بعد يومين
    Given مستخدم بحث عن "تلفزيون سامسونج" منذ يومين
    And لم يشتري أي صفقة
    
    When يفتح التطبيق مجدداً
    Then يجب أن يرى إشعار: "3 صفقات جديدة لتلفزيون سامسونج 🔥"
    
    When يضغط على الإشعار
    Then يجب أن ينتقل لصفحة نتائج محدثة
    And يجب أن يرى الصفقات الجديدة مميزة بشارة "جديد"
    
  Business KPI:
    - Return Rate: +30% بسبب الإشعارات
    - Conversion from Notification: 15%
```

---

## <a id="uat-tests"></a>👥 4. اختبارات قبول المستخدم (UAT)

### 4.1 سيناريوهات المستخدم السعودي الحقيقي

#### UAT-001: البحث عن منتج شائع في رمضان
```yaml
Test ID: UAT-001
Tester: مستخدم سعودي حقيقي (ليس من الفريق)
Context: شهر رمضان - موسم شراء مرتفع

Scenario: "أبغى شاشة 55 بوصة 4K بسعر حلو وتوصيل قبل رمضان"

Steps:
  1. المستخدم يفتح ابخص
  2. يبحث عن "شاشة 55 بوصة 4K"
  3. يختار فلتر "الميزانية: 1500-2500 ريال"
  4. يختار فلتر "التوصيل: سريع (1-3 أيام)"
  
Expected User Feedback:
  - "هل وجدت ما تبحث عنه؟" → نعم/لا
  - "هل كانت المقارنة واضحة؟" → نعم/لا
  - "هل تثق في الصفقة الموصى بها؟" → 1-5
  - "هل ستستخدم ابخص مرة أخرى؟" → نعم/لا

Success Criteria:
  ✅ 80%+ يجدون ما يبحثون عنه
  ✅ 90%+ يقولون المقارنة واضحة
  ✅ 4.5/5 متوسط الثقة
  ✅ 85%+ سيستخدمون ابخص مرة أخرى

Business Impact: رضا المستخدم = الانتشار العضوي = نمو مستدام
```

#### UAT-002: مقارنة صفقتين متشابهتين
```yaml
Test ID: UAT-002
Tester: مستخدم عادي
Context: قرار شراء بين خيارين متقاربين

Scenario: "عندي خيارين: واحد أرخص بـ 100 ريال بس عليه شحن 50، والثاني أغلى بس شحن مجاني. أيهما أحسن؟"

Expectation:
  - يجب أن يوضح ابخص التكلفة النهائية بشكل واضح جداً
  - يجب أن يبرز الصفقة الأفضل حسابياً
  
Test:
  المستخدم ينظر للصفقتين → هل يفهم الفرق في < 5 ثوانٍ؟

Success Criteria:
  ✅ 90%+ يفهمون الفرق بسرعة
  ✅ 85%+ يختارون الصفقة الأفضل حسابياً
  
Business Impact: وضوح المقارنة = قرارات أسرع = تحويل أعلى
```

#### UAT-003: التعامل مع عرض منتهي
```yaml
Test ID: UAT-003
Context: صفقة كانت متاحة أمس، انتهت اليوم

Scenario: مستخدم حفظ صفقة في المفضلة، عاد اليوم ليجدها منتهية

Steps:
  1. المستخدم يفتح "المفضلة"
  2. يضغط على صفقة محفوظة
  3. الصفقة انتهت أو الرابط لا يعمل
  
Expected Behavior:
  ✓ رسالة واضحة: "هذه الصفقة انتهت 😔"
  ✓ اقتراح فوري ببدائل مشابهة
  ✓ زر "ابحث عن صفقة بديلة"

User Feedback:
  - "هل كانت التجربة محبطة؟" → 1-5
  - "هل ساعدك الاقتراح البديل؟" → نعم/لا

Success Criteria:
  ✅ الإحباط < 2/5
  ✅ 70%+ يقبلون الاقتراح البديل

Business Impact: التعامل الجيد مع الفشل = الحفاظ على المستخدم رغم المشكلة
```

---

## <a id="negative-scenarios"></a>⚠️ 5. السيناريوهات السلبية (Negative Test Cases)

### 5.1 فشل الخدمات الخارجية

#### TC-NEG-001: انقطاع API متجر كامل
```typescript
Test ID: TC-NEG-001
Type: Resilience Test
Priority: P0 (Critical)

Given: API متجر "نون" لا يستجيب (Timeout or 500)

When: المستخدم يبحث عن "ساعات ذكية"

Then:
  ✓ التطبيق يستمر بالعمل (لا crash)
  ✓ يعرض نتائج من المتاجر الأخرى (جرير، أمازون، إكسترا)
  ✓ يظهر رسالة: "بعض المتاجر غير متوفرة حالياً، نعمل على حل المشكلة"
  ✓ يُسجّل الخطأ في Sentry لفريق DevOps
  ✓ يُرسل تنبيه للفريق التقني

Assert:
  - App Uptime: 100% (رغم فشل API)
  - User Experience: متوسط إلى جيد
  - Error Logged: ✅

Business Impact: 
  - بدون معالجة: خسارة 40% من المستخدمين عند الفشل
  - مع معالجة: خسارة < 5% فقط
```

#### TC-NEG-002: بيانات API غير صحيحة
```typescript
Test ID: TC-NEG-002
Type: Data Validation Test

Given: API يرجع بيانات خاطئة:
  - سعر منتج = 0 ريال
  - سعر منتج = -100 ريال
  - سعر منتج = 99,999,999 ريال
  - اسم المنتج: null أو ""
  - رابط الصورة: مكسور

When: النظام يستقبل هذه البيانات

Then:
  ✓ يجب رفض البيانات غير الصحيحة (Validation)
  ✓ يجب عدم عرض المنتج في النتائج
  ✓ يجب تسجيل الخطأ: "Invalid data from API"
  ✓ يجب إرسال تنبيه لفريق التكامل

Validation Rules:
  - price > 0 && price < 1,000,000
  - name.length > 0
  - image_url is valid URL

Business Impact: عرض بيانات خاطئة = فقدان الثقة الكامل
```

### 5.2 سلوك المستخدم غير المتوقع

#### TC-NEG-003: بحث بكلمات غير ذات صلة
```typescript
Test ID: TC-NEG-003
Type: Edge Case

Given: مستخدم يبحث عن كلمات غريبة:
  - "asdfghjkl"
  - "😀😀😀"
  - "       " (مسافات فقط)
  - كلمة طويلة جداً (500 حرف)

When: النظام يعالج البحث

Then:
  ✓ لا يحدث crash
  ✓ يعرض رسالة: "لم نجد نتائج، جرّب كلمات أخرى"
  ✓ يقترح كلمات شائعة: "ايفون، سامسونج، سماعات، شاشات"
  ✓ يُسجّل البحث لتحليل سلوك المستخدمين

Business Impact: تعامل سلس مع الحالات الشاذة = تجربة احترافية
```

#### TC-NEG-004: ضغط سريع متكرر على زر البحث
```typescript
Test ID: TC-NEG-004
Type: Stress Test - UI

Given: مستخدم يضغط على زر "البحث" 10 مرات في ثانية واحدة

When: النظام يستقبل الطلبات

Then:
  ✓ يجب تطبيق Debouncing (تجاهل الطلبات المكررة)
  ✓ يجب تنفيذ طلب واحد فقط
  ✓ لا يحدث تعليق أو crash
  ✓ لا يتم استدعاء API 10 مرات (منع الهدر)

Technical Solution:
  - Debounce delay: 300ms
  - Rate limiting على API calls

Business Impact: حماية من الاستخدام الخاطئ + توفير تكاليف API
```

### 5.3 مشاكل الشبكة والأداء

#### TC-NEG-005: إنترنت بطيء جداً (3G)
```typescript
Test ID: TC-NEG-005
Type: Performance Test - Poor Network
Tool: Chrome DevTools (Throttling: Slow 3G)

Given: مستخدم لديه إنترنت بطيء (3G)

When: يحاول فتح صفحة نتائج البحث

Then:
  ✓ يجب أن تظهر "Skeletons" مباشرة (Loading State)
  ✓ يجب أن تُحمّل النصوص قبل الصور
  ✓ يجب أن تعمل الكاش للزيارات المتكررة
  ✓ الوقت الأقصى: 5 ثوانٍ (بدلاً من 2 في الحالة الطبيعية)

Assert:
  - No timeout errors
  - Progressive loading: ✅
  - User sees feedback: ✅

Business Impact: 25% من المستخدمين السعوديين لديهم إنترنت متوسط السرعة
```

#### TC-NEG-006: فقدان الاتصال أثناء التصفح
```typescript
Test ID: TC-NEG-006
Type: Offline Behavior Test

Given: مستخدم يتصفح التطبيق

When: ينقطع الإنترنت فجأة

Then:
  ✓ يجب أن يعرض رسالة: "لا يوجد اتصال بالإنترنت"
  ✓ يجب أن يستمر عرض البيانات المحفوظة في الكاش
  ✓ يجب تعطيل الأزرار التي تحتاج إنترنت
  ✓ عند عودة الإنترنت: تحديث تلقائي

Offline Features:
  - عرض آخر النتائج المحفوظة
  - عرض المفضلة (من Local Storage)

Business Impact: تجربة جيدة حتى في الظروف السيئة = +10% Retention
```

---

## 📊 ملخص الأولويات

### حالات الاختبار الحرجة (P0 - يجب أن تنجح 100%)
1. ✅ TC-E2E-001: رحلة البحث والنقر على صفقة
2. ✅ TC-UNIT-001: حساب السعر الصافي
3. ✅ TC-UNIT-007: توليد روابط الأفلييت
4. ✅ TC-NEG-001: معالجة فشل API
5. ✅ TC-INT-003: تتبع Conversions

### حالات الاختبار عالية الأهمية (P1 - نجاح 95%+)
1. TC-E2E-002: الفلترة والترتيب
2. TC-UNIT-002: الترتيب حسب الأولوية
3. TC-UAT-001: رضا المستخدم السعودي
4. TC-NEG-002: رفض البيانات الخاطئة

### حالات الاختبار متوسطة الأهمية (P2 - نجاح 90%+)
1. TC-E2E-003: الإشعارات والعودة
2. TC-UNIT-005: البحث مع أخطاء إملائية
3. TC-UAT-003: التعامل مع العروض المنتهية

---

## 🎯 خطة التنفيذ

### الأسبوع الأول
- ✅ كتابة وتنفيذ جميع Unit Tests (TC-UNIT-001 إلى 007)
- ✅ إعداد بيئة الاختبار

### الأسبوع الثاني
- ✅ كتابة وتنفيذ Integration Tests
- ✅ إعداد Mock APIs للاختبار

### الأسبوع الثالث
- ✅ كتابة وتنفيذ E2E Tests
- ✅ إعداد أدوات الاختبار (Playwright/Cypress)

### الأسبوع الرابع
- ✅ إجراء UAT مع مستخدمين حقيقيين
- ✅ جمع Feedback وتحسين

### مستمر
- ✅ تنفيذ Negative Tests بشكل دوري
- ✅ إضافة حالات اختبار جديدة حسب الميزات

---

**النتيجة:** تغطية شاملة لكل الرحلات الحرجة = تطبيق موثوق = ثقة المستخدمين = إيرادات مستدامة
